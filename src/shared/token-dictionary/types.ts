export interface TokenDictionaryEntry {
  coingeckoId: string;
  name: string;
  symbol: string;
  marketCapRank: number;
}

export interface TokenDictionaryMatch {
  coingeckoId: string;
  name: string;
  symbol: string;
}
